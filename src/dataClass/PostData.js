/* eslint-disable */
class PostData {
  constructor(postId, content, categoryName, nickname, profileImageUrl) {
    ;(this.postId = postId),
      (this.content = content),
      (this.categoryName = categoryName),
      (this.nickname = nickname),
      (this.profileImageUrl = profileImageUrl)
  }
}
export default PostData
